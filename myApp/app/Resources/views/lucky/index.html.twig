{# app/Resources/views/blog/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}My cool blog posts{% endblock %}

{% block body %}

    <img src="{{ asset('images/rt.jpg') }}" />

    {{ path('luck', {'n': 111}) }}
    <hr />


    {% for n in numbers %}
        {{ n }}
    {% endfor %}

    {# for entry in blog_entries %}
        <h2>{{ entry.title }}</h2>
        <p>{{ entry.body }}</p>
    {% endfor %#}
{% endblock %}